// This file is generated by Ultramarine and should not be modified by hand
import React, { Fragment } from 'react'
import PropTypes from 'prop-types'
import Task from '../task'
import Input from '../../ui/Input'

const CreateFolder = ({
  folderPath,
  basePath,
  editable,
  onChange,
  ...rest
}) => (
  <Task editable={editable} {...rest} icon={CreateFolder.icon}>
    {editable ? (
      <Fragment>
		<Input
          id="folderPath"
          text="Folder Path"
          value={folderPath}
          onChange={onChange}
        />
		<Input
          id="basePath"
          text="Base Path"
          value={basePath}
          onChange={onChange}
        />
        
      </Fragment>
    ) : (
      <Fragment>
		<p>Folder Path: {folderPath}</p>
		<p>Base Path: {basePath}</p>
      </Fragment>
    )}
  </Task>
)

CreateFolder.description = 'Create Folder'
CreateFolder.icon = 'code'
CreateFolder.type = 'createFolder'

CreateFolder.propTypes = {
  folderPath: PropTypes.string,
  basePath: PropTypes.string,
  editable: PropTypes.bool,
  onChange: PropTypes.func
}

CreateFolder.defaultProps = {
  folderPath: '',
  basePath: '',
  editable: false,
  onChange: () => {}
}

export default CreateFolder
