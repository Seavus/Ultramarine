// This file is generated by Ultramarine and should not be modified by hand
import React, { Fragment } from 'react'
import PropTypes from 'prop-types'
import Task from '../task'
import Input from '../../ui/Input'

const BuildProject = ({
  projectName,
  configuration,
  editable,
  onChange,
  ...rest
}) => (
  <Task editable={editable} {...rest} icon={BuildProject.icon}>
    {editable ? (
      <Fragment>
		<Input
          id="projectName"
          text="Project Name"
          value={projectName}
          onChange={onChange}
        />
		<Input
          id="configuration"
          text="Configuration"
          value={configuration}
          onChange={onChange}
        />
        
      </Fragment>
    ) : (
      <Fragment>
		<p>Project Name: {projectName}</p>
		<p>Configuration: {configuration}</p>
      </Fragment>
    )}
  </Task>
)

BuildProject.description = 'Build Project'
BuildProject.icon = 'code'
BuildProject.type = 'buildProject'

BuildProject.propTypes = {
  projectName: PropTypes.string,
  configuration: PropTypes.string,
  editable: PropTypes.bool,
  onChange: PropTypes.func
}

BuildProject.defaultProps = {
  projectName: '',
  configuration: '',
  editable: false,
  onChange: () => {}
}

export default BuildProject
