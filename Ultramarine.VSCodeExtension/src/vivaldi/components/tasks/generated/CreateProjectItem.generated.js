// This file is generated by Ultramarine and should not be modified by hand
import React, { Fragment } from 'react'
import PropTypes from 'prop-types'
import Task from '../task'
import Input from '../../ui/Input'

const CreateProjectItem = ({
  itemName,
  folderPath,
  linkedWith,
  projectName,
  overwrite,
  editable,
  onChange,
  ...rest
}) => (
  <Task editable={editable} {...rest} icon={CreateProjectItem.icon}>
    {editable ? (
      <Fragment>
		<Input
          id="itemName"
          text="Item Name"
          value={itemName}
          onChange={onChange}
        />
		<Input
          id="folderPath"
          text="Folder Path"
          value={folderPath}
          onChange={onChange}
        />
		<Input
          id="linkedWith"
          text="Linked With"
          value={linkedWith}
          onChange={onChange}
        />
		<Input
          id="projectName"
          text="Project Name"
          value={projectName}
          onChange={onChange}
        />
		<Input
          id="overwrite"
          text="Overwrite"
          value={overwrite}
          onChange={onChange}
        />
        
      </Fragment>
    ) : (
      <Fragment>
		<p>Item Name: {itemName}</p>
		<p>Folder Path: {folderPath}</p>
		<p>Linked With: {linkedWith}</p>
		<p>Project Name: {projectName}</p>
		<p>Overwrite: {overwrite}</p>
      </Fragment>
    )}
  </Task>
)

CreateProjectItem.description = 'Create Project Item'
CreateProjectItem.icon = 'code'
CreateProjectItem.type = 'createProjectItem'

CreateProjectItem.propTypes = {
  itemName: PropTypes.string,
  folderPath: PropTypes.string,
  linkedWith: PropTypes.string,
  projectName: PropTypes.string,
  overwrite: PropTypes.string,
  editable: PropTypes.bool,
  onChange: PropTypes.func
}

CreateProjectItem.defaultProps = {
  itemName: '',
  folderPath: '',
  linkedWith: '',
  projectName: '',
  overwrite: '',
  editable: false,
  onChange: () => {}
}

export default CreateProjectItem
