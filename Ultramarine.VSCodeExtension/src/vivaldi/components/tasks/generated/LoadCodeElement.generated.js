// This file is generated by Ultramarine and should not be modified by hand
import React, { Fragment } from 'react'
import PropTypes from 'prop-types'
import Task from '../task'
import Input from '../../ui/Input'

const LoadCodeElement = ({
  elementName,
  elementType,
  elementAccess,
  elementOverride,
  typeOf,
  editable,
  onChange,
  ...rest
}) => (
  <Task editable={editable} {...rest} icon={LoadCodeElement.icon}>
    {editable ? (
      <Fragment>
		<Input
          id="elementName"
          text="Element Name"
          value={elementName}
          onChange={onChange}
        />
		<Input
          id="elementType"
          text="Element Type"
          value={elementType}
          onChange={onChange}
        />
		<Input
          id="elementAccess"
          text="Element Access"
          value={elementAccess}
          onChange={onChange}
        />
		<Input
          id="elementOverride"
          text="Element Override"
          value={elementOverride}
          onChange={onChange}
        />
		<Input
          id="typeOf"
          text="Type Of"
          value={typeOf}
          onChange={onChange}
        />
        
      </Fragment>
    ) : (
      <Fragment>
		<p>Element Name: {elementName}</p>
		<p>Element Type: {elementType}</p>
		<p>Element Access: {elementAccess}</p>
		<p>Element Override: {elementOverride}</p>
		<p>Type Of: {typeOf}</p>
      </Fragment>
    )}
  </Task>
)

LoadCodeElement.description = 'Load Code Element'
LoadCodeElement.icon = 'code'
LoadCodeElement.type = 'loadCodeElement'

LoadCodeElement.propTypes = {
  elementName: PropTypes.string,
  elementType: PropTypes.string,
  elementAccess: PropTypes.string,
  elementOverride: PropTypes.string,
  typeOf: PropTypes.string,
  editable: PropTypes.bool,
  onChange: PropTypes.func
}

LoadCodeElement.defaultProps = {
  elementName: '',
  elementType: '',
  elementAccess: '',
  elementOverride: '',
  typeOf: '',
  editable: false,
  onChange: () => {}
}

export default LoadCodeElement
