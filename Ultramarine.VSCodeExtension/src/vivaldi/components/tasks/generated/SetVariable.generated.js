// This file is generated by Ultramarine and should not be modified by hand
import React, { Fragment } from 'react'
import PropTypes from 'prop-types'
import Task from '../task'
import Input from '../../ui/Input'

const SetVariable = ({
  variableName,
  variableValue,
  parentTask,
  editable,
  onChange,
  ...rest
}) => (
  <Task editable={editable} {...rest} icon={SetVariable.icon}>
    {editable ? (
      <Fragment>
		<Input
          id="variableName"
          text="Variable Name"
          value={variableName}
          onChange={onChange}
        />
		<Input
          id="variableValue"
          text="Variable Value"
          value={variableValue}
          onChange={onChange}
        />
		<Input
          id="parentTask"
          text="Parent Task"
          value={parentTask}
          onChange={onChange}
        />
        
      </Fragment>
    ) : (
      <Fragment>
		<p>Variable Name: {variableName}</p>
		<p>Variable Value: {variableValue}</p>
		<p>Parent Task: {parentTask}</p>
      </Fragment>
    )}
  </Task>
)

SetVariable.description = 'Set Variable'
SetVariable.icon = 'code'
SetVariable.type = 'setVariable'

SetVariable.propTypes = {
  variableName: PropTypes.string,
  variableValue: PropTypes.string,
  parentTask: PropTypes.string,
  editable: PropTypes.bool,
  onChange: PropTypes.func
}

SetVariable.defaultProps = {
  variableName: '',
  variableValue: '',
  parentTask: '',
  editable: false,
  onChange: () => {}
}

export default SetVariable
