// This file is generated by Ultramarine and should not be modified by hand
import React, { Fragment } from 'react'
import PropTypes from 'prop-types'
import Task from '../task'
import Input from '../../ui/Input'

const StringManipulation = ({
  format,
  type,
  pattern,
  replacement,
  editable,
  onChange,
  ...rest
}) => (
  <Task editable={editable} {...rest} icon={StringManipulation.icon}>
    {editable ? (
      <Fragment>
		<Input
          id="format"
          text="Format"
          value={format}
          onChange={onChange}
        />
		<Input
          id="type"
          text="Type"
          value={type}
          onChange={onChange}
        />
		<Input
          id="pattern"
          text="Pattern"
          value={pattern}
          onChange={onChange}
        />
		<Input
          id="replacement"
          text="Replacement"
          value={replacement}
          onChange={onChange}
        />
        
      </Fragment>
    ) : (
      <Fragment>
		<p>Format: {format}</p>
		<p>Type: {type}</p>
		<p>Pattern: {pattern}</p>
		<p>Replacement: {replacement}</p>
      </Fragment>
    )}
  </Task>
)

StringManipulation.description = 'String Manipulation'
StringManipulation.icon = 'code'
StringManipulation.type = 'stringManipulation'

StringManipulation.propTypes = {
  format: PropTypes.string,
  type: PropTypes.string,
  pattern: PropTypes.string,
  replacement: PropTypes.string,
  editable: PropTypes.bool,
  onChange: PropTypes.func
}

StringManipulation.defaultProps = {
  format: '',
  type: '',
  pattern: '',
  replacement: '',
  editable: false,
  onChange: () => {}
}

export default StringManipulation
